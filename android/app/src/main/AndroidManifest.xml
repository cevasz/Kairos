<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Red: los mosaicos del mapa (OpenStreetMap) y, solo si hay clave de
         API, el texto del horario hacia la API de Claude. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Ubicación: en primer plano al tocar «Mi ubicación» o «Mi casa es
         aquí». En segundo plano solo si activas «Anotar falta si sigo en
         casa» en Ajustes: una comprobación por clase, 15 min después de su
         inicio. Android pide «Permitir todo el tiempo» aparte. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <!-- Los widgets se actualizan a la hora de salir y al empezar y terminar
         cada clase; las alarmas tienen que sobrevivir a un reinicio. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Crear alarmas en el Reloj del teléfono y avisar la víspera de una evaluación. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Actualizaciones sin cable: la app descarga la versión nueva de su
         Release en GitHub y abre el instalador de Android. Android pide
         permiso la primera vez. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="Kairós"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- «¿Sigues en casa?»: la comprobación, el «Sí fui» de la
             notificación y el rearmado tras reiniciar. -->
        <receiver android:name=".AttendanceCheckReceiver" android:exported="false"/>
        <receiver android:name=".AttendanceUndoReceiver" android:exported="false"/>
        <receiver android:name=".AttendanceBootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <!-- Comparte con el instalador solo el APK descargado en cache/updates. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths"/>
        </provider>
        <!-- Widgets de la pantalla de inicio. Los datos los deja la app en
             SharedPreferences (lib/features/widgets/home_widget_sync.dart). -->
        <receiver
            android:name=".NextClassWidgetProvider"
            android:label="@string/widget_picker_next"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next_class_info"/>
        </receiver>
        <receiver
            android:name=".TodayWidgetProvider"
            android:label="@string/widget_picker_today"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_info"/>
        </receiver>
        <receiver
            android:name=".EvalReminderReceiver"
            android:exported="false" />
        <receiver
            android:name=".PendingWidgetProvider"
            android:label="@string/widget_picker_pending"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_pending_info"/>
        </receiver>
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetScheduledUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
        <!-- «Abrir la ruta» le pasa la dirección a la app de mapas. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
