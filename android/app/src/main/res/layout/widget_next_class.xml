<?xml version="1.0" encoding="utf-8"?>
<!-- Widget 2×2 del prototipo (1c): padding 16, borde izquierdo de 3 dp con el
     acento, fondo surface.widget. Urgente solo cambia número y borde. Debajo,
     la llegada estimada y Erizógenes en el pie, donde antes quedaba un hueco. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/widget_accent"
        android:layout_width="3dp"
        android:layout_height="match_parent"
        android:layout_marginVertical="12dp"
        android:importantForAccessibility="no"
        android:background="@color/kairos_surface_border" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="13dp"
        android:paddingEnd="16dp"
        android:paddingVertical="14dp">

        <include layout="@layout/widget_next_class_body" />

        <!-- Separador: RemoteViews no admite una View simple. -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <LinearLayout
            android:id="@+id/widget_footer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom">

            <ImageView
                android:id="@+id/widget_mascot"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:importantForAccessibility="no"
                android:scaleType="fitCenter" />

            <TextView
                android:id="@+id/widget_quip"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="6dp"
                android:textSize="11sp"
                android:textStyle="italic"
                android:textColor="@color/kairos_text_secondary"
                android:maxLines="2"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
